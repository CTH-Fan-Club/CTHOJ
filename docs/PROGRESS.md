# 进度

## 已完成

- 可启动 Monorepo、品牌和跨平台命令。
- 用户登录/注册/退出、HttpOnly Cookie、密码哈希、登录限流、RBAC。
- 题库、题目详情、代码编辑、提交、异步状态、提交详情和 SSE。
- `ExecutionProvider` 的 Judge0 真实适配器和明确隔离的开发 Mock Provider。
- OpenAI-compatible 诊断适配器与 AI 关闭降级。
- 比赛列表、排行榜、能力画像工作台。
- 管理员数据实验室运行、可复现 Seed、冲突、缩小结果和质量报告界面。
- Prisma 目标模型、Docker Compose、架构/API/安全文档和自动测试。

## 未完成

- Prisma/PostgreSQL 实际数据读写与迁移；BullMQ/Redis 实际队列；MinIO 对象上传。
- Judge0 和 OpenAI-compatible 服务的真实联机验证。
- 完整竞赛报名/罚时/封榜、Rating 更新。
- Playwright 端到端测试和生产部署加固。

## 已知限制

开发 Mock 判题只用于本地交互测试，不能作为真实判题结果；生产数据实验室应使用 Judge0 或其他隔离 `ExecutionProvider`。当前机器缺少 Docker，外部依赖未启动。

## 2026-08-15 AI 超时配置统一

- AI 生成、错误诊断和管理员“测试模型 API”请求默认超时统一为 30 分钟。
- `.env`、`.env.example`、本地 Judge0 配置脚本和自动化测试均明确设置 `AI_TIMEOUT_MS=1800000` 与 `AI_TEST_TIMEOUT_MS=1800000`。
- 测试进程显式关闭 AI 并清空模型配置，避免读取本地真实 `.env` 导致测试受外部服务影响。

## 2026-08-15 数据实验室语言联动

- 数据实验室为 C++17、C++20、Python 3 和 Java 17 提供成套 Generator、Validator、标程与暴力程序模板，切换语言时同步替换并保护已编辑源码。
- AI 策略请求携带目标语言和 stdin/stdout 协议约束，服务端移除 Markdown 代码围栏并拒绝明显的语言不匹配，避免将 Python 源码按 C++ 提交。
- 四种语言模板均完成本地编译或语法检查；自动化测试覆盖策略降级时的语言上下文保留。

## 2026-08-15 数据实验室完整数据导出

- 成功运行的每组完整输入保存到独立 `DATA_LAB_DATA_DIR`，运行记录只保留文件索引、预览、大小和哈希。
- 新增管理员 `GET /admin/data-lab/:runId/export`，导出包含 `manifest.json` 和 `cases/*.in` 的 ZIP；失败运行和旧的仅预览记录不可导出。
- 删除运行时同步清理独立数据目录，自动化测试覆盖导出内容和清理行为。

## 2026-08-15 AI 数据实验室完整闭环

- 将固定演示运行替换为可配置的真实流水线：Seed 驱动 Generator，Validator 输出 `VALID`，标程/暴力程序经 `ExecutionProvider` 隔离执行并按 Token 对拍。
- 增加有限次删行/删 Token 的反例缩小，报告合法性、边界覆盖、重复输入、对拍冲突和差异击杀率；运行记录不保存完整判题输出。
- 增加服务端 AI 测试策略接口、管理员每日配额、用量记录和 Prompt 版本创建/列表；AI 关闭、失败或超额时明确返回确定性降级结果。
- 增加 Base64 ZIP 中央目录审计，检查路径穿越、绝对路径、符号链接、加密条目、不支持压缩方式、ZIP64/大小限制和压缩炸弹，不解压或执行内容。
- 管理员数据实验室前端增加运行参数、四类程序编辑器、策略生成、运行报告、冲突展示和 ZIP 审计入口。
- 审计运行支持管理员删除，并同步清理索引产物与关联审计日志。
- 管理员 AI 实验室增加模型 API 连通性测试，服务端发送最小请求并返回状态、延迟和脱敏错误摘要。
- 自动化 API 测试显式关闭 AI 并清空模型配置，避免读取开发机 `.env` 导致测试结果受外部服务影响。
- 本地 Judge0 联机 Smoke Test：实验室 Generator/Validator/标程/暴力程序均成功执行，1 组无差异报告完成。

## 2026-08-14 验证结果

- `npm run typecheck`：通过。
- `npm run lint`：通过。
- `npm test`：3/3 通过，覆盖健康检查、Seed、登录 Cookie、异步提交与 RBAC。
- 浏览器桌面与 390×844 移动视口：布局通过，无控制台错误。
- 管理员登录、题目提交、异步状态、数据实验室运行：通过。
- Docker、真实 Judge0、真实 AI：当前环境未配置，未进行联机验证。

## 2026-08-14 服务器部署

- 已完成 Node.js + systemd + Nginx 的服务器部署验证，服务监听 80 端口。
- 服务器目录：`/opt/cthoj/current`；持久化数据：`/opt/cthoj/data`。
- 使用专用 `cthoj` 系统用户和 `CAP_NET_BIND_SERVICE`，服务已启用开机自启。
- 外部首页与健康检查返回 HTTP 200，浏览器无控制台错误。
- 服务器 Seed 默认密码与 JWT 密钥已轮换，README 中的开发密码不能登录公网实例。
- Docker Hub 在服务器网络上超时，因此当前使用服务器已有 Node 20 + systemd；后续可配置镜像加速后切换容器部署。
- 尚未配置域名与 HTTPS，公网实例暂不适合传输正式账号密码。

## 2026-08-14 基础设施迁移准备

- 已增加相互隔离的 CTHOJ PostgreSQL/Redis 与 Judge0 PostgreSQL/Redis/Server/Worker Compose 拓扑。
- Judge0 固定为 `v1.13.1`，默认单 Worker、禁止用户代码联网并启用 Token 认证。
- 已增加 Windows/Linux 初始化、启动、停止、健康检查和真实代码执行 Smoke Test。
- 已增加 PostgreSQL 备份/恢复、Redis 快照、无密钥迁移包和 SSH 一键服务器部署脚本。
- 初始检查时 VT-x/Docker 尚不可用，因此当时未完成联机验证；该环境阻塞已在下一阶段解决。

## 2026-08-15 本地基础设施部署

- 已确认 VT-x/Hypervisor 生效，安装 WSL 2.7.11 和 Docker Desktop 4.86.0。
- 已验证 Docker Desktop 程序、镜像、容器、卷和 WSL Swap 可迁移到非系统盘。
- CTHOJ PostgreSQL、CTHOJ Redis、Judge0 PostgreSQL、Judge0 Redis、Judge0 Server 和 Judge0 Worker 均已启动并通过容器健康检查。
- Judge0 API 报告版本 `1.13.1`，认证 Token 已轮换；Worker 启动日志已增加敏感变量脱敏。
- 真实 C++17 Smoke Test 已到达 Worker，但因 Docker Desktop WSL 后端使用 cgroup v2，官方 isolate 返回 `/box/main.cpp` 不存在；不能声称真实判题通过。
- 已启用 Windows Hyper-V 功能，系统要求重启后生效；重启后需切换 Docker Hyper-V/cgroup v1 并重新运行 Smoke Test。

## 2026-08-15 cgroup v2 判题验证

- 重启后确认 Hyper-V、`vmcompute` 与 `vmms` 正常；Docker 程序和 WSL 数据仍分别位于 E 盘，Hyper-V 测试磁盘位于 D 盘。
- Docker Desktop 4.86.0 的 WSL 与 Hyper-V 后端均固定为 cgroup v2，`DeprecatedCgroupv1` 已不再改变 LinuxKit 启动参数，因此放弃不可靠的 cgroup v1 路径。
- 新增 `cthoj/judge0-cgroupv2:1.13.1-isolate2.6` 派生镜像：固定 isolate 2.6 提交，应用 Judge0 PR #599 的入口、参数和内存指标兼容修改。
- PostgreSQL、Redis、Judge0 Server 与 Worker 全部通过健康检查；Judge0 API 版本为 1.13.1。
- 真实 C++17 Smoke Test 连续 3 次返回 `Accepted`，耗时约 0.005-0.007 秒，内存约 32MB。
- Worker 日志包含预期的 `[REDACTED]` 标记，生成的认证与数据库密钥泄漏计数为 0。
- Windows 与 Linux 启动脚本均会自动构建 cgroup v2 镜像，迁移源码包会包含完整构建上下文且继续排除密钥和运行数据。

## 2026-08-15 本地 OJ 接入 Judge0

- 新增 `scripts/configure-local-judge0.ps1`，从受保护的基础设施配置生成本地 `.env`，配置 Judge0、PostgreSQL、Redis 和独立随机 JWT 密钥，全程不输出密钥。
- 本地 CTHOJ 健康接口已从 `mock` 切换为 `judge0`。
- Judge0 适配器改用 Base64 传输源码、标准输入、预期输出和诊断，修复编译器输出含非 UTF-8 字节时被误报为 `System Error` 的问题。
- OJ API 端到端验证通过：正确 C++17 为 `Accepted`，非法语法为 `Compile Error`，错误输出为 `Wrong Answer`。

## 2026-08-15 用户菜单与用户中心

- 登录用户点击顶部头像后显示账户菜单，可进入用户中心或退出登录；访客点击仍打开登录窗口。
- 菜单支持 `Esc`、点击外部关闭，提供明确的 ARIA 菜单状态，并适配只显示头像的移动端顶栏。
- 新增用户中心视图，展示 Rating、全站排名、解题数、通过率、累计提交、最近提交、账户信息和题库训练进度。
- 修正编译错误、系统错误等状态徽标颜色；开发环境静态资源改为 `no-cache`，避免刷新后继续加载旧界面。
- 桌面与 390×844 移动视口验证通过，无横向溢出或控件重叠；登录、菜单开合、用户中心和退出流程均通过，浏览器控制台无错误。

## 2026-08-15 管理员新增题目与 Special Judge

- 新增仅管理员/出题人可见的“新增题目”工作区，可配置题面、Slug、标签、时间/内存限制、发布状态、样例与隐藏测试点。
- 支持精确匹配、忽略空白 Token、浮点误差和 C++17 自定义 Checker 四种评测方式；提交结果展示通过测试点数、总耗时和峰值内存。
- 选手代码与自定义 Checker 均通过 `ExecutionProvider` 在 Judge0 中隔离运行，API 进程不执行不受信任代码。
- 题库与题目详情响应会剥离隐藏测试数据和 Checker 源码，只公开评测模式、误差和测试点数量。
- 自定义 Checker 使用带 UTF-8 字节长度头的输入协议，输出 `AC` 表示接受；Checker 上限 100KB。
- 真实 Judge0 联机验证通过：自定义“任意偶数” Checker 对两个测试点均正确接受偶数输出并拒绝奇数输出。
- `node --check`、`npm run typecheck`、`npm run lint` 和自动化测试通过；管理员页面在桌面与 390×844 移动视口无横向溢出。

## 2026-08-15 隐藏测试数据扩容

- 移除隐藏测试点 10 组上限，管理员页面与服务端均允许不限组数。
- 单题样例和隐藏测试数据按 UTF-8 实际字节数累计，硬上限提高至 2GB；超过上限返回 HTTP 413。
- 新增逐测试点流式上传、未发布草稿发布与失败清理接口，避免将大型数据集中放入单个 JSON 请求。
- 隐藏测试数据独立保存到 `TEST_DATA_DIR`，主 JSON 只保存文件引用和字节数；评测时逐个加载测试点。
- 自动化测试覆盖 11 个隐藏测试点、12/12 聚合判题、隐藏数据脱敏和累计字节上限拒绝，7/7 通过。

## 2026-08-15 完整比赛系统

- 新增比赛创建、编辑、报名、详情、赛题入口和比赛上下文提交校验，支持开始时间、时长与结束前自动封榜分钟数。
- ACM 赛制按解题数与罚时排名，有效错误提交在最终通过后每次增加 20 分钟；编译错误和系统错误不计罚时。
- OI 赛制取每题最后一次提交分数，IOI 赛制取每题历史最高分；两者按通过测试点比例提供 0-100 部分分。
- 支持自动封榜、管理员手动封榜/解封、完整榜单和逐条滚榜；公开榜单在冻结期间隐藏未滚出的提交结果。
- 新增管理员“比赛管理”工作区和用户比赛详情/榜单页面，榜单显示题目状态、得分、罚时和冻结待定状态。
- PostgreSQL/Prisma 目标模型补充赛制、封榜状态、滚榜记录和比赛时间字段。
- 自动化测试覆盖 ACM 20 分钟罚时、报名与时间校验、封榜/滚榜，以及 OI 最后得分与 IOI 最佳得分，9/9 通过。

## 2026-08-15 Markdown 与 LaTeX 题面渲染

- 题目描述、输入格式和输出格式新增安全 Markdown 渲染，支持标题、段落、列表、引用、粗体、斜体、行内代码和代码块。
- 新增常用 LaTeX 数学表达式渲染，支持 `$...$`、`$$...$$`、`\\(...\\)`、`\\[...\\]`、分数、根号、上下标和常用希腊字母/运算符。
- 题面 HTML 始终先转义，链接仅允许 `https://`、`http://` 或站内 `#` 地址；不加载外部 CDN，离线部署也可用。
- 浏览器实际验证 A+B 题面中的 3 个行内数学片段和行内代码均正确渲染，原始 `$a$` 不再显示，桌面端无横向溢出。

## 2026-08-15 AI 数据实验室规模规划与完整答案

- 标程为每组输入保存对应 `.out`，导出 ZIP 同时包含配对的 `.in/.out` 和带哈希、大小、规模、密度及 Corner Case 元数据的 `manifest.json`。
- Generator 协议扩展为 `Seed Size Density Profile`，继续兼容只读取 Seed 的旧生成器；服务端校验精确规模、范围规模、密度和逐组类型。
- 管理员页面新增精确/大概规模切换、密度滑块、柱状分布、逐组规模编辑和 Corner Case 类型选择。

## 2026-08-15 比赛题目权限隔离

- 比赛详情和比赛列表不再向普通用户泄露未公开比赛的 `problemIds`。
- 管理员/出题人始终可以查看和管理赛题；普通用户仅在已报名且比赛开始后获得题目详情。
- 未公开阶段的榜单只显示题号列，不返回题目标题或题目 ID；前端显示题目待公布提示。
- 新增 API 回归测试覆盖普通用户、管理员和比赛开始后的已报名用户三种可见性状态。

## 2026-08-15 用户注册

- 登录弹窗新增登录/注册切换，可填写用户名、昵称、邮箱、密码和确认密码。
- 注册成功后自动登录，服务端仍只返回脱敏用户信息，不向浏览器暴露密码哈希。
- 注册接口增加用户名/邮箱格式校验、大小写不敏感的重复账号检查和频率限制。
- 自动化测试覆盖注册成功、重复账号和弱密码拒绝。

## 2026-08-15 新用户初始数据清空

- 移除仪表盘中的固定演示统计，今日提交、已解决题目、连续训练和近 7 天趋势均从当前用户提交记录计算。
- 能力画像和知识点掌握度改为按用户实际提交与题目标签计算；新用户显示 0 和暂无训练数据。
- 用户中心的已解决题目与训练进度同步使用真实提交结果，题库和比赛等公共数据保持可见。

## 2026-08-15 题目评论与内容治理

- 每道题新增评论区，评论使用与题面一致的安全 Markdown/LaTeX 渲染，并提供提交前预览。
- 服务端新增评论违禁词、可选 AI 自动审核、AI 额外关键词和管理员批量重扫；违规评论不会公开，已有违规评论采用软删除。
- 管理员内容管理页支持配置审核策略、扫描/删除评论、查看账号、1-36500 天或永久封禁/解封用户。
- 管理员可授予普通用户 ADMIN；撤销管理员权限和处理管理员账号仅允许 admin 用户或超级管理员。
- 用户封禁在登录和所有写操作服务端生效，不能通过绕过前端恢复操作权限。
- 自动化测试覆盖评论 Markdown/LaTeX 内容、关键词拒绝、批量扫描、管理员角色变更和封禁登录拦截。

## 2026-08-15 题目可见性与独立比赛排行榜

- 新增题目支持设置“对普通用户开放”；旧题目自动按可见处理，管理员/出题人可在题库和题目管理页切换可见状态。
- 普通用户对隐藏题目无法从题库枚举、访问详情、查看评论或提交代码；服务端接口统一执行权限校验。
- 比赛详情、比赛列表均提供独立排行榜入口，新增独立排行榜视图，保留 ACM/OI/IOI、封榜和滚榜状态展示。
- API 与前端语法检查、Lint、TypeScript 边界检查及题目可见性回归测试通过。

## 2026-08-15 站内通知系统

- 新增持久化站内通知，支持全部用户或指定用户发送，并按用户保存已读状态。
- 顶部通知中心显示未读数量，支持查看单条通知、单条已读和全部已读。
- 管理员新增“通知管理”页面，可填写标题、正文、类型并选择收件人。
- 服务端严格校验管理员权限、通知对象和正文长度；普通用户只能读取发给自己的通知。

## 2026-08-15 用户头像

- 用户中心新增头像上传、恢复默认头像功能，顶部账户菜单同步显示头像。
- 前端将图片裁剪压缩到 256×256；服务端仅接受 PNG、JPEG、WebP、GIF，校验文件头并限制 512KB。
- 头像文件独立保存到 `AVATAR_DIR`，用户数据仅保存安全文件引用和版本号。

## 2026-08-15 远程服务器部署

- 已在 Ubuntu 22.04 测试服务器安装 Docker Engine、Compose v2，并启用 Swap。
- 同机启动 CTHOJ PostgreSQL/Redis 与 Judge0 专用 PostgreSQL/Redis、Judge0 Server、单 Worker；容器健康检查全部通过。
- API 以 `cthoj-api` systemd 服务运行，使用 Judge0 provider、独立持久化数据目录和 30 分钟轮询上限；Nginx 监听 80 端口反向代理。
- 已完成外部 `/api/v1/health` 检查和 Judge0 C++17 真实编译执行烟测（Accepted）。
- 本阶段保留现有 JSON 文件存储模型；PostgreSQL/Redis 已部署为独立基础设施，但尚未改写业务读写层接入它们。
- 服务器约 2GB 内存仅适合低并发验证；Judge0 已限制为单 Worker，生产扩容应迁移到至少 2 核 4GB 的判题节点。

## 2026-08-16 仓库发布准备

- 重写项目 README，补充本地开发、Judge0、Docker 与 Ubuntu 生产部署流程。
- 扩充 Git/Docker 忽略规则，排除本地数据、运行配置、密钥、日志、迁移包和内部工程说明。
- 生产环境首次初始化改为必须通过 `CTHOJ_ADMIN_PASSWORD` 提供管理员密码，避免公开固定凭据。
